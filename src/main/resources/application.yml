spring:
  profiles:
    active: test
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true

server:
  port: 8081

security:
  jwt:
    secret: ovVBYAe9BR1e6pMD6SM4rZtbFvPzBXCV
    expiresInDays: 2
